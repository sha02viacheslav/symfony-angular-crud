<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="keywords" content="HTML5 Template" />
        <meta name="description" content="Webmin - Bootstrap 4 & Angular 5 Admin Dashboard Template" />
        <meta name="author" content="potenzaglobalsolutions.com" />
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
        <title>{% block title %}Purchase-It{% endblock %}</title>
        {% block stylesheets %}
            <link rel="shortcut icon" href="{{ asset('images/favicon.gif') }}" />
            <link  rel="stylesheet" href="https://fonts.googleapis.com/css?family=Poppins:200,300,300i,400,400i,500,500i,600,600i,700,700i,800,800i,900">
            <link rel="stylesheet" type="text/css" href="{{ asset('css/style.css') }}" />
            <link rel="stylesheet" type="text/css" href="{{ asset('css/custom.css') }}" />
        {% endblock %}
    </head>
    <body>
        {% block body %}
            <form method="post">
                <div class="wrapper">

                    <section class="height-100vh d-flex align-items-center page-section-ptb login" >
                        <div class="container">
                            <div class="row justify-content-center no-gutters vertical-align">

                            <div class="col-md-6 bg-white">
                                <div class="login-fancy pb-40 clearfix">
                                <h3 class="mb-30">Sign In To Admin</h3>
                                <div class="section-field mb-20">
                                    {% if error %}
                                        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                                    {% endif %}

                                    {% if app.user %}
                                        <div class="mb-3">
                                            You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                                        </div>
                                    {% endif %}
                                    <label class="mb-10" for="name">User name* </label>
                                    <input  class="web form-control" type="text" placeholder="User name" value="{{ last_username }}" name="email" id="inputEmail">
                                    </div>
                                    <div class="section-field mb-20">
                                        <label class="mb-10" for="Password">Password* </label>
                                        <input class="Password form-control" type="password" placeholder="Password" name="password" id="inputPassword">
                                        <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                                    </div>
                                    <div class="section-field">
                                    <div class="remember-checkbox mb-30">
                                        <input type="checkbox" class="form-control" id="remember_me" name="_remember_me" checked />
                                        <label for="remember_me"> Remember me</label>
                                        <a href="password-recovery.html" class="float-right">Forgot Password?</a>
                                        </div>
                                    </div>
                                    <button class="button" type="submit">
                                        <span>Log in</span>
                                    </button>

                                </div>
                                </div>
                            </div>
                        </div>
                    </section> 
                </div>
            </form>
        {% endblock %}
        {% block javascripts %}
            <script src="{{ asset('js/jquery-3.3.1.min.js') }}"></script>

            <!-- plugins-jquery -->
            <script src="{{ asset('js/plugins-jquery.js') }}"></script>

            <!-- plugin_path -->
            <script>var plugin_path = 'js/';</script>
            
            <!-- chart -->
            <script src="{{ asset('js/chart-init.js') }}"></script>

            <!-- calendar -->
            <script src="{{ asset('js/calendar.init.js') }}"></script>

            <!-- charts sparkline -->
            <script src="{{ asset('js/sparkline.init.js') }}"></script>

            <!-- charts morris -->
            <script src="{{ asset('js/morris.init.js') }}"></script>

            <!-- datepicker -->
            <script src="{{ asset('js/datepicker.js') }}"></script>

            <!-- sweetalert2 -->
            <script src="{{ asset('js/sweetalert2.js') }}"></script>

            <!-- toastr -->
            <script src="{{ asset('js/toastr.js') }}"></script>

            <!-- validation -->
            <script src="{{ asset('js/validation.js') }}"></script>

            <!-- lobilist -->
            <script src="{{ asset('js/lobilist.js') }}"></script>
            
            <!-- custom -->
            <script src="{{ asset('js/custom.js') }}"></script>
        {% endblock %}
    </body>
</html>



